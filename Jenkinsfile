pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // sh 'atlas-mvn -B -D skipTests clean package'
                echo 'hello world !'
            }
        }
    }
}